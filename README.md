# Lightweight Midi Library

This is a header-only (for now at least) C++-23 library intended for micro-controllers.  
The parser uses C++-23's "Explicit Object Parameter" feature (deducing this). So this library
will not be compatible with older compilers.

### Features
- Serial MIDI stream parser w/ sysex support

### TODO
- Unit Tests
- ???
